# Audio Sync Tool — Bağımlılıklar / Dependencies
# Python >= 3.10 gereklidir / Python >= 3.10 required

numpy>=1.24.0
scipy>=1.10.0

# Opsiyonel / Optional — exe derleme için / for exe compilation
# pyinstaller>=6.0.0
